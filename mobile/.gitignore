# Flutter / Dart build artifacts — generated by `flutter create` / `flutter build`
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
.pub-cache/
.pub/
*.iml
.idea/

# Generated platform folders are recreated with `flutter create .` (see README).
# Keep lib/, pubspec.yaml, and README.md — those are the source of truth.
android/
ios/
linux/
macos/
windows/
web/
